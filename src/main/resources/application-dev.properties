# Development Profile - Local Setup
# Use with: --spring.profiles.active=dev

# Use H2 in-memory database for quick testing (optional)
# Uncomment below and comment MySQL config to use H2
#spring.datasource.url=jdbc:h2:mem:testdb
#spring.datasource.driver-class-name=org.h2.Driver
#spring.h2.console.enabled=true
#spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# Disable Kafka for local dev (optional)
#spring.kafka.bootstrap-servers=
#spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.kafka.KafkaAutoConfiguration

# More verbose logging for development
logging.level.com.example.billing=DEBUG
logging.level.org.springframework.web=DEBUG
logging.level.org.hibernate.SQL=DEBUG

